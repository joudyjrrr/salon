
import { Typography } from '@mui/material';

const Fallback = () => {
    return (
        <>
            <Typography>
                Loading
            </Typography>

        </>
    )
}

export default Fallback