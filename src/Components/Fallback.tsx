
import { Typography } from '@mui/material';
import Loading from './Loading';

const Fallback = () => {
    return (
        <>
            {/* <Typography>
                Loading
            </Typography> */}
            <Loading/>
        </>
    )
}

export default Fallback